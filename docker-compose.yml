version: "3.8"

services:
  portfolio:
    image: nginx:alpine
    container_name: portfolio-rev0li
    restart: unless-stopped
    ports:
      - "3000:80"
    volumes:
      - ./static:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
